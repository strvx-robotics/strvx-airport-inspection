ultralytics==8.4.80
fastapi>=0.115
uvicorn[standard]>=0.30
pillow>=10.0
python-multipart>=0.0.9
huggingface_hub>=0.25

# live-feed worker (app/live/worker.py)
opencv-python>=4.8   # video-stream frame sampling (also a transitive ultralytics dep)
requests>=2.31       # worker -> /api/live-capture POST
anthropic>=0.40      # VLM marking/lighting detector (optional; needs ANTHROPIC_API_KEY)

# reinforcement-learning loop (rl/)
numpy>=1.26          # reward model + policies (also a transitive ultralytics dep)
